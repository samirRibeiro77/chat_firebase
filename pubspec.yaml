name: chat_firebase
description: Chat using Firebase API.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  cloud_firestore: ^0.11.0+2
  google_sign_in: ^4.0.4
  firebase_auth: ^0.11.1
  firebase_storage: ^3.0.0
  image_picker: ^0.6.0+3


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
